apiVersion: v1
kind: Service
metadata:
  name: auspost-devops-demo
  namespace: auspost-demo
  annotations:
    # Uses the EKS legacy service controller for a minimal assessment lab.
    # For production, prefer AWS Load Balancer Controller or EKS Auto Mode.
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
