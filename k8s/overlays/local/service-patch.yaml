apiVersion: v1
kind: Service
metadata:
  name: auspost-devops-demo
  namespace: auspost-demo
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
